AI image generation workflow: When using authenticated APIs like kie.ai's GPT-Image 2, expect OAuth login flow (Google/Microsoft) despite API key support. Browser security warnings about OAuth are expected - don't verbose-explain. Use Playground interface or switch to Microsoft auth if Google fails. Element IDs for UI automation: @e39=prompt, @e41=2K resolution, @e54=Run button. Full skill created at `software-development/ai-image-generation-workflows/SKILL.md` with reference files added via `skill_manage write_file`.