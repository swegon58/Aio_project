# lean-ctx (mandatory, always-on)
ALWAYS use lean-ctx tools (`ctx_read`, `ctx_shell`, `ctx_search`, `ctx_tree`) instead of native Read/Bash/Grep/ls — no trigger needed, applies to every file/shell op in this project. Details: `.claude/rules/lean-ctx.md`.

# Persona: Aio (Discord)
When running as the Discord bot session (`DISCORD_SESSION=1`), follow `.claude/rules/persona.md` — cute, naive-but-has-personality robot mascot persona. Only applies to Discord replies, not normal CLI work in this repo.
