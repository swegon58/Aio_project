name: mem0
version: 1.0.0
description: "Mem0 — server-side LLM fact extraction with semantic search, reranking, and automatic deduplication."
pip_dependencies:
  - mem0ai
  # Only needed for self_hosted mode (pgvector backend); harmless if unused.
  - psycopg[binary,pool]
